import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

// Rename to LibraryFirebaseModule
@NgModule({
  imports: [CommonModule],
})
export class LibraryFirebaseModule {}
